import { useState } from "react";
import { AvailablePage } from "../pages";

export default function CoopAvailable() {
  // const title = coop && <h2>{coop.name} Coop</h2>;
  return (
    <div>
      {/* {title} */}
      <AvailablePage />
    </div>
  );
}
